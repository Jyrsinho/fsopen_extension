import {describe, expect, test, beforeEach} from 'vitest'

describe('anecdoteService', () => {
    beforeEach(() => {
        global.fetch = vi.fn()
    })
    describe('getAll', () => {
        test('should ', () => {})
    })
})